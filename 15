// SPDX-License-Identifier: MIT
pragma solidity >= 0.5.0 < 0.9.0;

contract mulWithoutOperator {
    function MulWithoutOperator(int x, uint y) public pure returns(int) {
        int mul;
        for(uint i; i < y; i++) {
            mul = mul + x;
        }
        return mul;
    }
}
