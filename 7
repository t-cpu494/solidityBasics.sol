// SPDX-License-Identifier: GPL-3.0
pragma solidity >= 0.5.0 < 0.9.0;

contract Prime {
    function prime( uint x ) public pure returns( uint ) {
        for ( uint i = x/2; i > 1; i-- ) {
            if ( x % i == 0  ) return 0;
        }
        return 1;
    }
}
